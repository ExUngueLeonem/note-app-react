import React from 'react';

const Navbar = () => {
    return (
        <nav className="navbar navbar-dark navbar-expand-gl bg-primary">
            <div className="navbar-brand">            
                Note App
            </div>
        </nav>
    );
};

export default Navbar;